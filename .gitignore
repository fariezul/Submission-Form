# This is now a plain static site — there is nothing secret to ignore.
# The Supabase anon key in app.js is meant to be public.
#
# Keep this file around so Git ignores common junk:

node_modules/
.DS_Store
Thumbs.db

# Safety net: if you ever add a .env later, Git will never upload it.
.env
.env.*
!.env.example

# Charts belonging to other work that happen to sit in this folder.
# Listing them here keeps them off the website.
pareto-casting-defects.png
fishbone-diagram.jpg
histogram-petal-length.png
control-chart-xbar.png
